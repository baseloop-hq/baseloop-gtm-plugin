---
name: mini-plugin:foo
description: A tiny skill for testing transforms — has Task calls and slash references.
---

# Foo

Run `Task data-quality-auditor(check the data)` first.

Then `/baseloop-gtm:plan something` and `/baseloop-gtm:review` if needed.

Look in `.claude/cache/` for past artifacts.

@data-quality-auditor handles the rest.
