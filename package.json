{
  "name": "baseloop-gtm",
  "version": "0.8.2",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "references:sync": "bun run scripts/references/sync.ts",
    "references:check": "bun run scripts/references/sync.ts --check",
    "release:validate": "bun run scripts/release/validate.ts",
    "release:preview": "bun run scripts/release/preview.ts",
    "release:sync-metadata": "bun run scripts/release/sync-metadata.ts",
    "package:zip": "bun run scripts/package/zip.ts"
  },
  "bin": {
    "baseloop-plugin": "src/index.ts"
  },
  "dependencies": {
    "citty": "^0.1.6",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "bun-types": "^1.1.0"
  }
}
